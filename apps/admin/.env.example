DATABASE_URL = ""

# Supabase Authentication
NEXT_PUBLIC_SUPABASE_URL = "https://your-project-id.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY = "your-anon-key-here"
SUPABASE_SERVICE_ROLE_KEY = "your-service-role-key-here"

# Authentication Providers - set to 'true' to enable, 'false' to disable
NEXT_PUBLIC_ENABLE_GOOGLE_AUTH = "false"

# Admin emails - comma separated list of emails that have admin access
ADMIN_EMAILS = "admin@example.com,another-admin@example.com"

# Email Service (for transactional emails)
MAIL_SMTP_HOST = "smtp-relay.brevo.com"
MAIL_SMTP_PORT = "587"
MAIL_SMTP_USER = "your-brevo-login@smtp-brevo.com"
MAIL_SMTP_PASS = "your-brevo-api-key"
MAIL_FROM_EMAIL = "noreply@plottdesign.com"
MAIL_FROM_NAME = "PlottDesign"

# App Configuration
NEXT_PUBLIC_URL = "http://localhost:8888"

NODE_ENV = 'development'

# Cloudinary (for image uploads)
# Get cloud name from: https://cloudinary.com/console
# Create upload preset at: Settings → Upload → Add upload preset
# IMPORTANT: Set preset to "unsigned" mode
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME = ""
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET = "plottdesign"